# PIck 产品底稿

本文件记录已确认的产品方向、当前阶段与开放问题，不是完整需求规格、技术架构或书籍方法论 Wiki。

协作入口见 [AGENTS.md](../AGENTS.md)，详细开发约定见 [AI 开发协作](../ai-collaboration/README.md)。

## 已确认的长期方向

PIck 面向保研、留学等找导师场景，首先服务项目负责人的真实需求，并逐步支持其他用户使用。功能随实际需求、使用体验和对找导师问题的理解演进，不预先补齐整套平台。

- 普通用户通过云端使用，不涉及用户端安装、离线使用或本地与云端同步。
- 本地运行用于开发、验证和启动使用；项目需要有清晰、便于维护的云端部署方式，不能依赖个人电脑上的特定环境。
- 账号密码等注册登录能力属于已确认方向，现阶段使用 Supabase 邮箱密码认证。
- 大部分核心功能将参考用户提供的一本书的世界观与方法论；并非所有功能都必须来自该书，账号等通用能力无需方法论依据。
- 只实现当前确认的需求，同时控制后续增加、修改、替换和删除功能的成本。不为假想需求引入复杂架构。

## 三层产品逻辑

以下是职责和内容之间的关系，不代表三个独立系统，也不直接决定技术分层、服务拆分或数据库设计。

### 信息接入层

根据当前功能需要的信息，按需调研和连接外部网站、科研数据库等来源，不预先对接书中全部平台。

接入可以采用 API、文件导入、外链跳转或人工补充，不要求全部自动获取。重要信息应保留来源和获取时间，并区分已获得的信息与尚未核实的内容。

### 方法论与知识层

以 Wiki 为主要载体，逐步整理书中的概念、观点、具体方法、适用条件及其关系。它不是按章节复制的电子书，也不是与功能无关的帮助中心。

- 区分原书观点、用户的理解与补充、待讨论方案和已确认采用的做法，不混写为原书结论。
- 已采用的方法应关联原书依据，以及支持它的产品功能或人工操作；从相关功能也应能找到方法论说明。
- 方法可以由功能、检查清单、指南或人工判断提示支持，不要求全部自动化，不未经讨论转化为评分、排名或推荐算法。
- 在用户提供并共同确认书籍内容前，不编造具体观点，不默认方法适用于所有保研和留学场景。

产品方法论 Wiki 与 AI 开发协作规范分开组织。Wiki 的实际载体与发布方式尚未选定。

### 用户功能层

把必要信息和已确认的方法转化为用户可操作的功能与流程，服务找导师过程中的具体任务。具体功能逐项讨论，不从三层框架推导完整功能清单。

### 演进方式

每次围绕一个真实需求，确认相关方法、确定必要信息、实现足够小且可验收的功能，并补充对应 Wiki 说明。无需先接入全部数据、整理完整 Wiki 或结构化整本书。通用功能不强行关联书籍方法。

## 当前阶段与准备状态

当前处于最小工程底座实施阶段。用户已确认一次“选型—确认—搭建—验证”的任务，不再将准备工作拆成独立研究阶段。

已完成的准备：

- 只读检查当前目录，创建文档前未发现已有项目文件。
- 明确云端用户使用、本地开发验证的边界，以及三层产品逻辑和双方分工。
- 将已确认约定整理为三份互相链接的文档。
- 核实并确认 Next.js 官方 `with-supabase` starter、Supabase Free、新加坡测试项目和 Vercel Hobby。当前是个人非商业试验，测试环境暂不验证邮箱，本地与云端共用测试数据库。
- 在 PIck 内建立独立 Git 仓库，保存原始共识文档版本；不使用 `D:/` 上级仓库记录工程。

本轮实现范围：本地启动，注册/登录/退出与受保护页面，最小数据读写和用户隔离，受保护的云端测试部署，以及版本、配置、启动说明和必要验证。不建设完整 Wiki、数据接入平台、推荐算法、工作流、插件体系或多服务架构。

实施状态（2026-09-21）：应用代码、单表 SQL 迁移、浏览器与 API 权限验证脚本已写入。官方 starter 的 7 个认证与 UI 源文件已按固定提交下载复用，无需继续下载整包模板。

已检查：用户完成 `npm install` 并生成锁文件（用户提供的 npm 输出报告 0 个漏洞）；lint 无警告、TypeScript 检查和生产构建通过。生产服务器使用 3101 端口启动，主页/登录/注册返回 200，未登录访问受保护页面返回 307 并跳转到登录页。直接依赖许可已核实为 MIT 或 Apache-2.0。另检查了两份 Node 验证脚本语法、14 个本地文档链接、独立 Git 根目录及凭据/产物忽略规则。

浏览器检查：已确认本机安装 Chrome 和 Edge，Playwright 成功复用 Chrome 的独立临时配置，无需额外下载 Chromium。`npm test` 实际执行 2 项检查并全部通过：主页及注册登录入口、未登录访问私有页被重定向。

真实服务检查：Supabase 测试项目、邮箱密码认证与数据库迁移已配置。用户用两个测试账号完成注册、登录、保存、刷新和退出，并确认双方记录相互不可见。`npm run test:isolation` 连接真实数据 API 后四组检查全部通过：双方只能读写自己的临时记录；匿名 SELECT/INSERT/UPDATE/DELETE 被拒绝；伪造归属、跨用户删除和所有 UPDATE 被拒绝；本人删除成功。脚本仅清理本次随机 ID 的临时记录。

云端准备：Vercel CLI 已登录并将本地目录关联到 `qianjiayus-projects/pi-ck`；Preview 环境已配置 Supabase URL 与 publishable key。本地 Vercel 关联文件和凭据继续由 Git 忽略。

仍待完成：尚未创建 Preview 部署地址；Vercel 访问保护和部署后的完整流程未验证。下一步只继续云端测试部署，不重新选型或扩展底座。

代码写入不等于运行通过。工程说明中的命令用途和外部操作见 [README](../README.md)，此处只记录实际阶段状态。书籍内容仍未提供，产品方法条目与首个找导师业务任务仍未确定。

底座范围完成后结束基础建设；后续基础设施由实际业务需求驱动。AI 开发实践随开发按需补充，不作为独立研究项目。

## 开放问题

- 首个真实用户任务是什么：用户已明确暂缓讨论，无需在本阶段追问完整功能清单。
- 参考书籍及相关内容、适用场景与具体采用的方法是什么。
- 具体业务所需的信息来源与接入方式是什么。
- 何时正式向其他用户开放；届时的邮箱验证、备份、商业用途托管与运行要求如何落实。
- Wiki 由谁编辑、面向谁发布，以及相关内容的引用和公开范围。

这些问题按近期任务需要逐步收敛，不要求在开发前全部解决。此前讨论中未获明确确认的实现建议，不视为已采用的技术方案。
